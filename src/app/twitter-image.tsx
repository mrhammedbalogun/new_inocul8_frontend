// Twitter/X large-summary card reuses the default Open Graph image so link
// previews render identically across social platforms and AI chat clients.
export { default, alt, size, contentType } from "./opengraph-image";
